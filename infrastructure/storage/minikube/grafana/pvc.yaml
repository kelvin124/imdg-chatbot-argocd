apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc
  namespace: monitoring
spec:
  storageClassName: "grafana"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi