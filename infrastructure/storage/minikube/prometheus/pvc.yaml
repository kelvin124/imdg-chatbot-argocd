apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-server-pvc
  namespace: monitoring
spec:
  storageClassName: "prometheus"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
